import { PaymentModeAndDates } from './payment-mode-and-dates';

describe('PaymentModeAndDates', () => {
  it('should create an instance', () => {
    expect(new PaymentModeAndDates()).toBeTruthy();
  });
});
